<!DOCTYPE html>
<html>
	<head>
		<meta charset = "utf-8" />
	</head>
	<body></body>
	<script>
		//question from kecek in fodler GDP question 2
		function calculate(cost = 0, price = 0, sum = 0, listSteel){
			var c = cost, p = price, n = sum;
			var list = listSteel;

			
		}	

		function test(){
			var data = [0.1, 0.1, 0.5, 0.5, 0.2, 0.1, 0.1, 0.5, 0.7, 0.6];
				


			return aloneValue(data);
		}

		function aloneValue(data){
			var tmp = data;
			var value = 0;
			var stop = true;
			for(var i = 0; i < tmp.length; i++){
				value = tmp[i];
				stop = true;
				//console.log(value);
				for(var k = 0; k < tmp.length; k++){
				
					if(i === k){
						continue;
					}
					//console.log(tmp[i] + " === " + tmp[k]);
					if(tmp[i] === tmp[k]){
						//console.log(tmp[i] + " === " + tmp[k]);
						//value = 0;
						stop = false;
						break;
					}
				}

				if(stop === true){
					break;
				}
			}		
			return value;
		}

		//test();
		console.log(test());

	</script>
</html>
